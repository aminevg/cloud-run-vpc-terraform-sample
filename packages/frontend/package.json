{
  "name": "frontend",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bunx --bun astro dev",
    "build": "bunx --bun astro build",
    "preview": "bunx --bun astro preview",
    "prod": "bun run --bun dist/server/entry.mjs",
    "astro": "bunx --bun astro"
  },
  "dependencies": {
    "@astrojs/node": "^9.2.2",
    "astro": "^5.10.1"
  }
}